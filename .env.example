# ──────────────────────────────────────────────────
# tvplus-mcp-chatwoot — Environment Configuration
# ──────────────────────────────────────────────────

# ─── Required: Chatwoot API ──────────────────────
CHATWOOT_BASE_URL=https://your-chatwoot.com
CHATWOOT_API_TOKEN=your_api_access_token_here

# ─── Optional: Default Account ──────────────────
# CHATWOOT_ACCOUNT_ID=1   # If omitted, account_id is required on every tool call

# ─── Transport ───────────────────────────────────
MCP_MODE=stdio                # stdio (default) | http
PORT=3000                     # HTTP server port (http mode only)
AUTH_TOKEN=                   # Bearer token for HTTP mode (recommended)

# ─── Logging ─────────────────────────────────────
LOG_LEVEL=info                # debug | info | warn | error

# ─── Safety ─────────────────────────────────────
MCP_SAFE_MODE=false           # true → blocks 20 destructive core/enterprise/helpcenter tools

# ─── Optional Buckets ───────────────────────────
# Public API (12 tools) — widget/client-facing, no api_access_token
MCP_ENABLE_PUBLIC_API=false

# Platform API (17 tools) — super-admin operations
MCP_ENABLE_PLATFORM_API=false
CHATWOOT_PLATFORM_API_TOKEN=  # Required if MCP_ENABLE_PLATFORM_API=true
MCP_PLATFORM_SAFE_MODE=true   # true (default) → blocks 11 platform write/delete tools

# Enterprise (8 tools) — audit logs, reporting events, account agent bots
MCP_ENABLE_ENTERPRISE=false

# Help Center (15 tools) — portals, articles, categories
MCP_ENABLE_HELP_CENTER=false

# ─── Smoke Test (optional) ──────────────────────
# LIVE_SMOKE=true             # Set to run live smoke tests
# CHATWOOT_PUBLIC_INBOX_IDENTIFIER=  # For public API smoke tests
